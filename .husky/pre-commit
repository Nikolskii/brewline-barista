# Только форматирование изменённых файлов: быстро и чинится само.
# Линт и типы сюда сознательно не вынесены — коммит это личный чекпоинт,
# незавершённый код в нём нормален. Строгие проверки живут в CI на pull request.
npx lint-staged
